<div align="center">

# NyashBoard ♡

**Социальная платформа с настоящей анонимностью, анонимными каналами и умной лентой**

Ванильный JavaScript · Firebase · без сборки · без фреймворков

</div>

---

## Изменения с момента последней публикации

Всё перечисленное ещё не выложено — накопилось за несколько итераций после версии, которая сейчас развёрнута.

### Новые разделы

**Друзья и личные чаты**
- Односторонняя дружба: добавив человека, видишь его записи в ленте и открываешь ему свой NUID
- Список друзей полностью приватный, читается только владельцем
- Взаимность определяется точечным запросом «а добавили ли меня» — без раскрытия чужого списка
- Личные чаты с превью последнего сообщения; открываются только при взаимном добавлении
- Вкладку можно отключить в настройках

**Хештеги**
- `#тег` в тексте становится ссылкой на страницу со всеми записями по этому тегу
- `#U1666777` распознаётся как идентификатор и ведёт на профиль, а не на страницу тега

**Умная лента**
- Записи подписок и непрочитанное поднимаются наверх по прозрачной системе очков
- Отметка «прочитано» ставится, когда запись пробыла на экране около двух секунд
- Прочитанное уезжает вниз, но не исчезает; сброс — в настройках
- Отключается переключателем, тогда лента сортируется просто по времени

**Рабочие настройки** (раньше были заглушкой)
- Три темы и три акцентных цвета, комбинируются между собой
- Падающие частицы: звёздочки, цветочки, листики или выключить
- Источник эмодзи: Noto с CDN, Apple локально или системные
- Свой порядок вкладок: на телефоне слева направо, на компьютере сверху вниз
- Относительное или точное время у записей и сообщений

### Оформление

- Отдельный макет для компьютера: навигация в колонке слева, лента сеткой на 2–3 колонки, редактор записи карточкой по центру
- Навигация собирается из одного модуля вместо копии в каждом файле
- Эмодзи в интерфейсе заменены глифами Monaspace Neon NF
- Иконки увеличены, отдельные кнопки правки и удаления заменены меню «⋮» с подписями
- Фон с падающими частицами

### Профили

- Описание профиля
- Эмодзи-статус в углу аватарки
- Выбор формы аватарки: круг, скруглённый квадрат, squircle
- Ручное кадрирование с зумом и перетаскиванием
- Прозрачные PNG не заливаются белым фоном
- Настройка видимости NUID: все, только друзья, никто
- Двухступенчатый просмотр чужого профиля: карточка с превью, затем полная страница

### Записи и чат

- До 10 фотографий в записи горизонтальной каруселью, со стрелками на компьютере
- Редактирование и удаление своих записей, ответов и сообщений — включая гостей без аккаунта
- Фотографии и эмодзи в ответах
- Сворачивание длинных записей
- Ответы на сообщения в чате с цитатой и переходом к оригиналу
- Реддит-подобное превью трёх самых оценённых ответов под записью
- Отдельная страница записи с полным обсуждением

### Безопасность и приватность

- **NUID больше не приходит в браузер, если владелец закрыл доступ.** Идентификатор вынесен из профиля в отдельную коллекцию; браузер запрашивает его отдельно, а решение принимает база. До ответа показывается маска `U1••••••`
- **Обратный индекс для поиска по NUID** разрешает точечное обращение и запрещает перечисление коллекции: зная идентификатор, человека найдёшь — собрать чужие не выйдет
- **Подписки на каналы переехали в аккаунт** и синхронизируются между устройствами, оставаясь скрытыми от администрации каналов
- Юзернеймы каналов теперь можно менять (правило не разрешало это поле)
- Сессия переживает перезагрузку страницы и переключение вкладок

### Исправления

- Ответы под записями больше не загружаются все разом при открытии ленты — было 50 запросов к базе за раз
- Вкладка «Люди» перестала вычитывать всех пользователей без ограничения
- Чат больше не прокручивается вниз, когда читаешь историю
- Клик по автору записи заработал на страницах канала и профиля
- Разорвана циклическая зависимость между модулями ленты и людей
- Панель ввода в чате перестала перехватывать нажатия по сообщениям над ней
- Фотографии в карусели выравниваются по центру, а не по верхнему краю
- Понятные сообщения об ошибках при неудачной загрузке изображений вместо `undefined`
- Скроллбары в палитре сайта, поля профиля одинаковой ширины, поиск по NUID во вкладках

### Инфраструктура

- **Firebase Storage убран**: с конца 2024 года Google требует привязанный биллинг даже для создания хранилища. Вместо него цепочка бесплатных хостов ImgBB → Catbox → Uguu с автоматическим переключением при отказе
- Изображения сжимаются перед отправкой и загружаются параллельно
- `deploy.sh` и `setup-termux.sh` — публикация с телефона одной командой

## Возможности

### Лента
Посты с текстом и до 10 фотографий в горизонтальной карусели. Лайки, ответы, репосты ссылкой (не копией). Публикация под своим именем, анонимно или от имени канала. Длинные посты сворачиваются, чтобы один пост не занимал весь экран.

Умная лента ранжирует посты по прозрачной системе очков:

| Условие | Очки |
|---|---|
| Пост канала из подписок, ещё не прочитан | +1000 |
| Любой другой непрочитанный | +300 |
| Пост канала из подписок (уже прочитанный) | +200 |
| Свежесть, затухает за неделю | до +250 |
| Уже прочитан | −700 |

Поэтому посты подписок оказываются наверху даже если они старые. Отметка «прочитано» ставится, когда пост побыл на экране около двух секунд, а не просто пролетел при скролле. Порядок пересчитывается только при загрузке страницы, чтобы лента не прыгала во время чтения.

### Чат
Общий чат с полной анонимностью. Личность в чате не связана с аккаунтом на уровне схемы данных: пока сам не назовёшь свой юзернейм, связать сообщения с профилем нечем. Поддерживаются фотографии, эмодзи, смена ника в один клик и ответы на сообщения с цитатой и переходом к оригиналу.

### Каналы
Анонимные публичные страницы, которыми может управлять несколько человек.

- Создатель канала нигде не хранится в самом документе канала — связь лежит в отдельной коллекции, недоступной на чтение никому, кроме него самого
- Админы могут публиковать, но не могут менять название, юзернейм, аватарку, описание и состав админов
- Подписка и видимость подписки — разные вещи, и хранятся раздельно:
  - **личный список подписок** лежит в приватной коллекции аккаунта, читать её может только сам владелец; синхронизируется между устройствами
  - **список подписчиков канала** — отдельная коллекция, её видит администрация канала
- Кнопка «Скрыться» удаляет запись из второго списка **на сервере**, а не прячет её в интерфейсе. Подписка при этом остаётся: канал продолжает попадать в ленту, просто администрация больше не знает о подписчике
- Удалить свою запись может только сам человек: администрация не может ни принудительно скрыть кого-то, ни раскрыть

### Друзья
Дружба односторонняя, как подписка: добавив человека, его записи попадают в твою ленту, и он получает доступ к твоему NUID при режиме «только друзья».

Список друзей лежит в приватной подколлекции и читается только владельцем. Прятаться, как в каналах, тут не от кого — никто и так не видит, кто у кого в друзьях. При этом правила базы могут проверить наличие записи, не раскрывая список: именно на этом работает выдача NUID друзьям.

Личный чат открывается только при взаимности. Проверить её можно, не раскрывая ничего лишнего: правила разрешают прочитать в чужом списке ровно один документ — свой собственный, то есть узнать «а добавили ли меня». Перечислить список целиком по-прежнему может только владелец. Дополнительно взаимность проверяется правилами при создании чата, поэтому написать постороннему нельзя даже в обход интерфейса.

### Профили
Ник, юзернейм, описание, аватарка с ручным кадрированием и выбором формы (круг, скруглённый квадрат, squircle), эмодзи-статус в углу аватарки, неизменный NUID с настройкой видимости.

Аватарка хранится необрезанной, поэтому форму можно менять когда угодно задним числом без перезаливки фото. Прозрачные PNG не заливаются белым фоном.

### Идентификаторы
- **FUID** — внутренний идентификатор Firebase, длинная случайная строка
- **NUID** — публичный человекочитаемый ID: `U1xxxxxx` у пользователей, `U4xxxxxx` у каналов

Юзернеймы уникальны для пользователей и каналов через общий реестр. Уникальность гарантируется атомарно на уровне правил базы, а не проверкой в коде, поэтому две одновременные регистрации одного ника не могут пройти обе.

Юзернеймы каналов всегда начинаются с `ch_`.

### Упоминания и хештеги
`@юзернейм` и `@ch_канал` кликабельны и ведут на профиль или канал. `#хештег` открывает страницу со всеми постами по этому тегу. `#U1666777` распознаётся как NUID и ведёт на аккаунт, а не на страницу тега.

---

## Технологии

| Слой | Решение |
|---|---|
| Фронтенд | Ванильный JS (ES-модули), без сборки и фреймворков |
| База и авторизация | Firebase Firestore + Firebase Auth |
| Картинки | ImgBB → Catbox → Uguu (цепочка с автоматическим переключением) |
| Шрифт интерфейса | Monaspace Neon NF |
| Эмодзи | Noto Color Emoji с CDN, Apple локально или системные |
| Хостинг | Любой статический (GitHub Pages, Netlify, Firebase Hosting) |

Firebase Storage намеренно не используется: с конца 2024 года Google требует привязанный биллинг даже для создания хранилища, поэтому в качестве бесплатного решения он не подходит.

---

## Установка

### 1. Firebase

1. Создать проект на [console.firebase.google.com](https://console.firebase.google.com)
2. **Authentication → Sign-in method** — включить **Google** и **Anonymous** (анонимный вход обязателен: на нём держится вся система владения контентом для гостей)
3. **Firestore Database → Create database** — режим production
4. **Project settings → Your apps** — добавить веб-приложение и скопировать конфиг
5. **Firestore → Rules** — вставить содержимое `firestore.rules` и опубликовать

### 2. Ключи

Заполнить `js/config.js`:

```js
export const FIREBASE_CONFIG = { /* конфиг из шага 1.4 */ };
export const IMGBB_API_KEY = "ключ с api.imgbb.com";  // необязательно
export const IMAGE_HOSTS = ["imgbb", "catbox", "uguu"];
```

Без ключа ImgBB его просто пропустят — остальные хосты работают без регистрации.

### 3. Запуск

Нужен любой локальный http-сервер: открытие `index.html` файлом не сработает, браузеры блокируют ES-модули по протоколу `file://`.

```bash
python -m http.server 8080
# или
npx serve .
```

### 4. Публикация

Подойдёт любой статический хостинг. После деплоя нужно добавить домен в **Firebase → Authentication → Settings → Authorized domains**, иначе вход через Google выдаст `unauthorized-domain`.

---

## Деплой с телефона

Обновление сайта одной командой из Termux, без компьютера.

### Настройка (один раз)

Скачать архив со сборкой, распаковать и запустить установщик:

```bash
cd $HOME
unzip -o storage/downloads/NyashBoard.zip
bash nyashboard/setup-termux.sh https://github.com/USERNAME/nyashboard.git
```

Установщик поставит нужные пакеты, запросит доступ к памяти телефона, настроит git и склонирует репозиторий.

При первом отправке изменений вместо пароля нужно вставить **Personal Access Token**: GitHub → Settings → Developer settings → Personal access tokens → Fine-grained, права `Contents: Read and write`. Токен запомнится и больше спрашиваться не будет.

### Обновление

```bash
bash $HOME/nyashboard/deploy.sh "что изменилось"
```

Скрипт сам найдёт свежий архив в загрузках, распакует его поверх репозитория, закоммитит и отправит на GitHub. Файл `js/config.js` при этом сохраняется — ключи не затираются содержимым архива. Если установлен `firebase-tools`, правила базы задеплоятся автоматически при их изменении.

### Если что-то не работает

**`permission denied` при запуске**

Скрипт нужно запускать через `bash script.sh`, а не `./script.sh`. Если файл лежит на общей памяти (`/sdcard`, папка загрузок), Android физически не позволяет ставить бит исполнения — `chmod +x` там молча ничего не делает. Запуск через `bash` обходит это ограничение.

**Репозиторий на общей памяти**

Репозиторий должен лежать во внутренней памяти Termux — в `$HOME`, то есть `/data/data/com.termux/files/home/`. На `/sdcard` нет полноценных прав доступа, из-за чего git ведёт себя непредсказуемо. Скрипт проверяет это и предупредит.

**Скрипт не находит архив**

Проверяются `~/storage/downloads`, `~/storage/shared/Download`, `/sdcard/Download` и `/storage/emulated/0/Download`. Если ни одна папка не видна, доступ к памяти не выдан:

```bash
termux-setup-storage
```

Разрешение нужно подтвердить во всплывающем окне Android.

**Свой путь к репозиторию**

```bash
NYASH_REPO=/путь/к/репозиторию bash deploy.sh "коммит"
```

## Архитектурные решения

**Владение контентом работает и для гостей.** Каждый посетитель получает анонимную сессию Firebase со своим идентификатором. При создании поста рядом пишется секрет-документ, недоступный на чтение вообще никому — он используется только правилами базы при проверке прав на изменение и удаление. Поэтому редактировать и удалять свои записи может даже человек без аккаунта.

**Скрытие NUID работает на уровне базы, а не интерфейса.** Идентификатор хранится не в профиле (тот читается публично целиком), а в отдельной коллекции с правилом, которое сверяется с настройкой приватности владельца. Браузер запрашивает значение отдельно; если доступ закрыт, база отвечает отказом и значение не попадает на устройство запросившего вообще. До ответа на экране показывается маска вида `U1••••••` — это заглушка, а не спрятанное настоящее значение.

Обратный индекс `NUID → профиль` позволяет найти человека по известному идентификатору, но правила разрешают только точечное обращение и запрещают перечисление коллекции. Получается ровно та асимметрия, ради которой NUID и нужен: дал кому-то свой идентификатор — тебя найдут, не дал — не узнают.

**Подписки привязаны к аккаунту, но анонимность не страдает.** Личный список подписок лежит в приватной подколлекции пользователя — по правилам базы читать её может только сам владелец, включая администрацию каналов. Поэтому список синхронизируется между устройствами и при этом никому не виден. Видимость подписки для админов — это отдельная запись в коллекции канала, которой управляют кнопки «Скрыться» и «Проявиться». У гостей без аккаунта подписки хранятся в браузере и переносятся в аккаунт при первом входе.

**Отметки «прочитано» хранятся локально.** Запись на сервер означала бы отдельную запись в базу на каждый показ каждого поста каждому человеку — самый быстрый способ исчерпать бесплатный лимит. Цена решения: на другом устройстве лента снова покажется новой.

**Ответы под постами грузятся лениво,** по мере появления карточек на экране. Загрузка всех сразу давала 50 запросов к базе при каждом открытии ленты.

**Запросы избегают составных индексов.** Фильтрация и сортировка по разным полям в одном запросе требует индекса, который Firestore не создаёт автоматически — вместо этого сортировка выполняется на клиенте.

**Навигация собирается из одного модуля.** Шапка не дублируется по страницам, поэтому её правка не требует одинаковых изменений в десяти файлах.

**Модули не зависят друг от друга циклически.** Лента не вызывает модуль людей напрямую, а отправляет событие — зависимость односторонняя.

---

## Ограничения

- **Владение гостевым контентом теряется при последующем входе** в аккаунт: Firebase выдаёт другой идентификатор сессии. Решается привязкой аккаунта к анонимной сессии вместо замены
- **Нет модерации и ограничения частоты запросов.** Для публичного запуска стоит подключить Firebase App Check
- **У гостей без аккаунта подписки остаются только в браузере** — синхронизировать их не к чему; при входе в аккаунт они автоматически переносятся
- **Лента без постраничной подгрузки** — последние 50 постов
- **Формат Apple-эмодзи не поддерживается Safari.** Там подставятся системные эмодзи, которые выглядят так же

---

## Структура

```
├── index.html              лента
├── chat.html               общий чат
├── content.html            каналы
├── people.html             люди
├── post.html               отдельный пост с полным тредом
├── channel.html            страница канала
├── my-channels.html        свои каналы
├── profile.html            свой профиль
├── user.html               чужой профиль
├── tag.html                посты по хештегу
├── friends.html            друзья и личные чаты
├── dm.html                 переписка с другом
├── settings.html           настройки
├── firestore.rules         правила доступа к базе
├── setup-termux.sh         первичная настройка Termux
├── deploy.sh               деплой из Termux одной командой
└── js/
    │
    │   ── ядро ──
    ├── firebase.js         инициализация SDK
    ├── config.js           ключи (единственный файл, который нужно заполнить)
    ├── auth.js             авторизация, выпадашка профиля
    ├── data.js             пользователи, реестр юзернеймов
    ├── nuid.js             идентификаторы, приватность, индекс поиска
    ├── storage.js          загрузка изображений с переключением хостов
    ├── ownership.js        локальные отметки владения контентом
    ├── identity.js         гостевая личность для чата
    ├── ui.js               уведомления, экранирование, даты
    │
    │   ── лента и посты ──
    ├── feed.js             лента, редактор постов
    ├── ranking.js          ранжирование умной ленты
    ├── seen.js             отметки «прочитано»
    ├── replies.js          ответы на посты
    ├── post.js             страница отдельного поста
    ├── carousel.js         карусель фотографий
    ├── hashtags.js         извлечение тегов из текста
    │
    │   ── чат ──
    ├── chat.js             сообщения, ответы, цитаты
    │
    │   ── каналы ──
    ├── friends.js          друзья, приватный список
    ├── dm.js               личные чаты
    ├── channels.js         создание, роли, видимость подписки
    ├── subscriptions.js    личные подписки (аккаунт + оффлайн-кэш)
    ├── channel.js          страница канала
    ├── content.js          каталог каналов, поиск, рекомендации
    ├── my-channels.js      свои каналы
    │
    │   ── люди и профили ──
    ├── people.js           список людей, карточка профиля
    ├── profile.js          редактирование своего профиля
    ├── user.js             чужой профиль
    ├── avatar.js           формы аватарок, кадрирование
    │
    │   ── интерфейс ──
    ├── layout.js           навигация, фон с частицами
    ├── icons.js            глифы Nerd Font
    ├── kebab.js            контекстное меню
    ├── emoji.js            пикер эмодзи
    ├── mentions.js         упоминания и хештеги
    ├── settings.js         хранение и применение настроек
    ├── settings-ui.js      интерфейс настроек
    │
    │   ── точки входа (по одной на страницу) ──
    └── *-page.js           feed, chat, content, people, post,
                            channel, my-channels, profile, user,
                            tag, settings
```

Точки входа намеренно тонкие: они только собирают нужные модули для конкретной страницы. Вся логика живёт в модулях выше и переиспользуется между страницами.
